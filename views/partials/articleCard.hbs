<div class="col">
    <div class="card shadow-sm">

        <svg class="bd-placeholder-img card-img-top" width="100%" height="225" xmlns="http://www.w3.org/2000/svg"
            role="img" aria-label="Placeholder: Thumbnail" preserveAspectRatio="xMidYMid slice" focusable="false">
            <title>{{this.title}}</title>
            <image href="/images/{{this.imagePath}}" alt="{{this.imageDescription}}" width="100%" height="100%"
                class="img-fluid" />
        </svg>

        <div class="card-body">
            <h2 class="card-text py-1 text-capitalize">{{this.title}}</h2>
            <div class="d-flex justify-content-between align-items-center">
                <a href="blog/{{this._id}}" class="btn btn-md btn-outline-secondary">Read More</a>
                <small class="text-muted">{{this.date}}</small>
            </div>
        </div>
    </div>
</div>